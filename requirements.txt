# Minimal runtime requirements for the House Price Estimator Streamlit app.
# (Trimmed from the full project's requirements.txt -- this app only needs
# to LOAD already-trained models and run inference, not retrain anything,
# so xgboost/lightgbm/catboost/optuna/lime/tabulate/h2o are NOT needed here.)
streamlit>=1.38
pandas>=1.5
numpy>=1.23
scikit-learn==1.8.0
matplotlib>=3.6
seaborn>=0.12
joblib>=1.3
imbalanced-learn>=0.11
shap>=0.44
pydantic>=2.0
google-genai>=1.0
